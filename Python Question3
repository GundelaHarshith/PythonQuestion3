def distance(word_1, word_2):
    if(len(word_1)!=len(word_2)):
      return -1
      
    distance = 0 
    for i in range(len(word_1)):
      chr1 = ord(word_1[i])
      chr2 = ord(word_2[i])
      temp = abs(chr1-chr2)
      distance += temp
    
    return distance
